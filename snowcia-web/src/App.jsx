import { useCallback, useEffect, useMemo, useState } from 'react'
import './App.css'

const API_URL = import.meta.env.VITE_API_URL ?? 'http://localhost:8081'
const navigation = [['Visão geral', '⌂'], ['Meus pets', '♧'], ['Reservas', '▣'], ['Pagamentos', '◈']]
const species = ['DOG', 'CAT', 'BIRD', 'OTHER']
const initialData = { pets: [], reservations: [], payments: [] }
const services = [
  ['HOSTING_24H', 'Hospedagem (diária 24h)', 'R$ 50 - R$ 75/dia'],
  ['DAYCARE_HALF_DAY', 'DayCare - meio período', 'R$ 30/dia'], ['DAYCARE_FULL_DAY', 'DayCare - período integral', 'R$ 45/dia'],
  ['DAYCARE_EXTRA_HOUR', 'DayCare - hora adicional', 'R$ 5/dia'], ['DAYCARE_EXTRA_WALK', 'DayCare - passeio extra', 'R$ 10/dia'],
  ['WALK_20_MIN', 'Passeio (20 min)', 'R$ 20/dia'], ['CAT_SITTER_DAILY', 'CatSitter - diária (2 visitas)', 'R$ 25/dia'],
  ['CAT_SITTER_ADDITIONAL_CAT', 'CatSitter - gato adicional', 'R$ 15/dia'], ['CAT_SITTER_EXTRA_VISIT', 'CatSitter - visita adicional', 'R$ 10/dia'],
  ['MONTHLY_DAYCARE_2X', 'Plano DayCare 2x/semana', 'R$ 330/mês'], ['MONTHLY_DAYCARE_3X', 'Plano DayCare 3x/semana', 'R$ 480/mês'], ['MONTHLY_DAYCARE_5X', 'Plano DayCare 5x/semana', 'R$ 780/mês'],
  ['MONTHLY_WALKS_2X', 'Plano Passeios 2x/semana', 'R$ 150/mês'], ['MONTHLY_WALKS_3X', 'Plano Passeios 3x/semana', 'R$ 215/mês'], ['MONTHLY_WALKS_5X', 'Plano Passeios 5x/semana', 'R$ 340/mês'],
]

function App() {
  const [token, setToken] = useState(() => localStorage.getItem('snowcia_token'))
  const [role, setRole] = useState(() => localStorage.getItem('snowcia_role') ?? 'CLIENT')
  const [isRegistering, setIsRegistering] = useState(false)
  const [authForm, setAuthForm] = useState({ name: '', email: '', phone: '', password: '' })
  const [error, setError] = useState(''); const [loading, setLoading] = useState(false)
  const [active, setActive] = useState('Visão geral'); const [data, setData] = useState(initialData); const [editor, setEditor] = useState(null)
  const isAdmin = role === 'ADMIN'

  const request = useCallback(async (path, options = {}) => {
    const response = await fetch(`${API_URL}${path}`, { ...options, headers: { Authorization: `Bearer ${token}`, ...(options.body ? { 'Content-Type': 'application/json' } : {}), ...options.headers } })
    const payload = response.status === 204 ? null : await response.json().catch(() => ({}))
    if (!response.ok) throw new Error(payload.message ?? 'Não foi possível concluir esta ação.')
    return payload
  }, [token])
  const logout = useCallback(() => { localStorage.removeItem('snowcia_token'); localStorage.removeItem('snowcia_role'); setToken(null); setRole('CLIENT'); setData(initialData); setError('') }, [])
  const refresh = useCallback(async () => {
    if (!token) return
    try { const [pets, reservations, payments] = await Promise.all([request('/api/pets'), request('/api/reservations'), isAdmin ? request('/api/payments') : Promise.resolve([])]); setData({ pets, reservations, payments }) }
    catch (e) { setError(e.message) }
  }, [isAdmin, request, token])
  useEffect(() => { refresh(); const timer = setInterval(refresh, 20000); return () => clearInterval(timer) }, [refresh])

  async function handleAuth(event) { event.preventDefault(); setLoading(true); setError(''); try { const endpoint = isRegistering ? '/api/auth/register' : '/api/auth/login'; const body = isRegistering ? authForm : { email: authForm.email, password: authForm.password }; const response = await fetch(`${API_URL}${endpoint}`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) }); const payload = await response.json(); if (!response.ok) throw new Error(payload.message ?? 'Não foi possível acessar sua conta.'); const nextRole = payload.role ?? 'CLIENT'; localStorage.setItem('snowcia_token', payload.token); localStorage.setItem('snowcia_role', nextRole); setRole(nextRole); setToken(payload.token) } catch (e) { setError(e.message) } finally { setLoading(false) } }
  async function saveEditor(form) { setLoading(true); setError(''); try { const { type, item } = editor; if (type === 'pet') await request(item ? `/api/pets/${item.id}` : '/api/pets', { method: item ? 'PUT' : 'POST', body: JSON.stringify(form) }); if (type === 'reservation') await request(item ? `/api/reservations/${item.id}` : '/api/reservations', { method: item ? 'PUT' : 'POST', body: JSON.stringify({ ...form, petId: Number(form.petId) }) }); setEditor(null); await refresh() } catch (e) { setError(e.message) } finally { setLoading(false) } }
  async function deleteItem(type, id) { if (!window.confirm('Deseja cancelar esta solicitação?')) return; try { await request(`/api/${type}/${id}`, { method: 'DELETE' }); refresh() } catch (e) { setError(e.message) } }
  async function decideReservation(id, action) { let body; if (action === 'decline') { const reason = window.prompt('Informe o motivo da recusa:'); if (!reason?.trim()) return; body = JSON.stringify({ reason }) } try { await request(`/api/reservations/${id}/${action}`, { method: 'PATCH', body }); refresh() } catch (e) { setError(e.message) } }
  async function updatePayment(id, action) { try { await request(`/api/payments/${id}/${action}`, { method: 'PATCH' }); refresh() } catch (e) { setError(e.message) } }
  if (!token) return <AuthScreen {...{ isRegistering, setIsRegistering, form: authForm, setForm: setAuthForm, onSubmit: handleAuth, loading, error }} />
  const upcoming = data.reservations.filter((r) => new Date(`${r.checkInDate}T12:00`) >= new Date()).length; const paid = data.payments.filter((p) => p.status === 'PAID').length
  return <main className="app-shell"><aside className="sidebar"><div className="brand"><span>✦</span> snowcia</div><nav>{navigation.filter(([label]) => label !== 'Pagamentos' || isAdmin).map(([label, icon]) => <button key={label} className={active === label ? 'nav-link active' : 'nav-link'} onClick={() => setActive(label)}><span>{icon}</span>{label}</button>)}</nav><button className="logout" onClick={logout}>↪ Sair</button></aside><section className="content"><header><div><p className="eyebrow">{isAdmin ? 'PAINEL ADMINISTRATIVO' : 'PAINEL DO CLIENTE'}</p><h1>{active}</h1></div><div className="avatar">🐾</div></header>{error && <p className="form-error page-error">{error}</p>}{active === 'Visão geral' && <Dashboard data={data} upcoming={upcoming} paid={paid} isAdmin={isAdmin} onReserve={() => setActive('Reservas')} />}{active === 'Meus pets' && <Pets pets={data.pets} openEditor={setEditor} onDelete={(id) => deleteItem('pets', id)} />}{active === 'Reservas' && <Reservations {...{ reservations: data.reservations, pets: data.pets, isAdmin, openEditor: setEditor, onDelete: (id) => deleteItem('reservations', id), decideReservation }} />}{active === 'Pagamentos' && isAdmin && <Payments payments={data.payments} updatePayment={updatePayment} />}</section>{editor && <Editor editor={editor} pets={data.pets} onClose={() => setEditor(null)} onSave={saveEditor} loading={loading} />}</main>
}

function AuthScreen({ isRegistering, setIsRegistering, form, setForm, onSubmit, loading, error }) { return <main className="auth-shell"><section className="brand-panel"><div className="brand"><span>✦</span> snowcia</div><div className="hero-copy"><p className="eyebrow">HOTEL PET, SEM COMPLICAÇÃO</p><h1>Todo o cuidado que o seu pet merece.</h1><p>Solicite serviços, acompanhe sua reserva e receba as atualizações da administração.</p></div></section><section className="auth-panel"><form className="auth-card" onSubmit={onSubmit}><p className="eyebrow">BEM-VINDO</p><h2>{isRegistering ? 'Crie sua conta' : 'Entre na sua conta'}</h2>{isRegistering && <><Field label="Nome"><input required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} /></Field><Field label="WhatsApp"><input required value={form.phone} onChange={(e) => setForm({ ...form, phone: e.target.value })} placeholder="5511999999999" /></Field></>}<Field label="E-mail"><input required type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} /></Field><Field label="Senha"><input required type="password" minLength="8" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} /></Field>{error && <p className="form-error">{error}</p>}<button className="primary-button" disabled={loading}>{loading ? 'Aguarde...' : isRegistering ? 'Criar conta' : 'Entrar'}</button><p className="switch-auth">{isRegistering ? 'Já possui uma conta?' : 'Ainda não tem uma conta?'} <button type="button" onClick={() => setIsRegistering(!isRegistering)}>{isRegistering ? 'Entrar' : 'Criar agora'}</button></p></form></section></main> }

function Dashboard({ data, upcoming, paid, isAdmin, onReserve }) { return <><div className="welcome"><div><p>Olá! Que bom te ver por aqui.</p><h2>{isAdmin ? 'Acompanhe as estadias confirmadas.' : 'Vamos cuidar de momentos incríveis?'}</h2>{!isAdmin && <button className="secondary-button" onClick={onReserve}>Solicitar serviço <span>→</span></button>}</div><span className="welcome-pet">🐶</span></div><div className="stats"><Stat icon="🐾" label="Pets cadastrados" value={data.pets.length} tone="blue" /><Stat icon="▣" label="Reservas futuras" value={upcoming} tone="yellow" />{isAdmin && <Stat icon="◈" label="Pagamentos confirmados" value={paid} tone="orange" />}</div>{isAdmin ? <ReservationCalendar reservations={data.reservations.filter((r) => ['AWAITING_PAYMENT', 'CONFIRMED'].includes(r.status))} /> : <section className="list-card"><SectionTitle title="Minhas solicitações" /><p className="inline-tip">Os status são atualizados automaticamente nesta tela após a decisão da administração.</p>{data.reservations.length ? data.reservations.slice(0, 4).map((r) => <ReservationRow key={r.id} reservation={r} />) : <Empty text="Nenhuma solicitação registrada." />}</section>}</> }
function ReservationCalendar({ reservations }) { const [cursor, setCursor] = useState(() => new Date()); const days = useMemo(() => calendarDays(cursor), [cursor]); return <section className="list-card calendar-card"><div className="section-heading"><div><p className="eyebrow">AGENDA</p><h2>{cursor.toLocaleDateString('pt-BR', { month: 'long', year: 'numeric' })}</h2></div><div className="row-actions"><button onClick={() => setCursor(new Date(cursor.getFullYear(), cursor.getMonth() - 1, 1))}>←</button><button onClick={() => setCursor(new Date(cursor.getFullYear(), cursor.getMonth() + 1, 1))}>→</button></div></div><div className="calendar-week">{['Dom', 'Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb'].map((d) => <span key={d}>{d}</span>)}</div><div className="calendar-grid">{days.map((day) => <div className={`calendar-day ${day.getMonth() === cursor.getMonth() ? '' : 'muted-day'}`} key={day.toISOString()}><strong>{day.getDate()}</strong>{reservations.filter((r) => between(day, r.checkInDate, r.checkOutDate)).map((r) => <small className="calendar-pet" key={r.id}>🐾 {r.petName}</small>)}</div>)}</div>{!reservations.length && <p className="inline-tip">As reservas aparecem aqui após a confirmação do pagamento.</p>}</section> }
function Pets({ pets, openEditor, onDelete }) { return <section className="list-card"><SectionHeading title="Seus companheiros" action="Cadastrar pet" onAction={() => openEditor({ type: 'pet' })} />{pets.length === 0 ? <Empty text="Nenhum pet cadastrado ainda." /> : <div className="item-grid">{pets.map((pet) => <article className="pet-card" key={pet.id}><div className="pet-emoji">🐾</div><h3>{pet.name}</h3><p>{labelOf(pet.species)} {pet.breed && `· ${pet.breed}`}</p><div className="card-actions"><button onClick={() => openEditor({ type: 'pet', item: pet })}>Editar</button><button className="danger" onClick={() => onDelete(pet.id)}>Excluir</button></div></article>)}</div>}</section> }
function Reservations({ reservations, pets, isAdmin, openEditor, onDelete, decideReservation }) { const groups = isAdmin ? [['PENDING', 'Pendentes'], ['AWAITING_PAYMENT', 'Aguardando pagamento'], ['CONFIRMED', 'Confirmadas'], ['DECLINED', 'Recusadas']] : [['ALL', 'Minhas solicitações']]; return <>{!isAdmin && <section className="list-card service-catalog"><SectionHeading title="Catálogo de serviços" action="Solicitar serviço" disabled={!pets.length} onAction={() => openEditor({ type: 'reservation' })} /><div className="catalog-grid">{services.map(([id, name, price]) => <div key={id}><strong>{name}</strong><small>{price}</small></div>)}</div></section>}{groups.map(([status, title]) => { const rows = status === 'ALL' ? reservations : reservations.filter((r) => r.status === status); return <section className="list-card reservation-section" key={status}><SectionTitle title={title} />{rows.length ? rows.map((r) => <ReservationDetail key={r.id} reservation={r} isAdmin={isAdmin} onEdit={() => openEditor({ type: 'reservation', item: r })} onDelete={() => onDelete(r.id)} onApprove={() => decideReservation(r.id, 'approve')} onDecline={() => decideReservation(r.id, 'decline')} />) : <Empty text={`Nenhuma reserva ${title.toLowerCase()}.`} />}</section> })}</> }
function ReservationDetail({ reservation, isAdmin, onEdit, onDelete, onApprove, onDecline }) { return <div className="reservation-row detailed"><div className="pet-dot">🐾</div><div><strong>{reservation.petName} · {serviceName(reservation.serviceType)}</strong><small>{formatDate(reservation.checkInDate)} {formatTime(reservation.checkInTime)} — {formatDate(reservation.checkOutDate)} {formatTime(reservation.checkOutTime)} · {formatCurrency(reservation.totalAmount)}</small>{isAdmin && <small className="note">Cliente: {reservation.ownerName} · {reservation.ownerPhone || reservation.ownerEmail}</small>}{reservation.notes && <small className="note">{reservation.notes}</small>}{reservation.declineReason && <small className="note decline-reason">Motivo da recusa: {reservation.declineReason}</small>}</div><span className={`status ${reservation.status.toLowerCase()}`}>{labelOf(reservation.status)}</span><div className="row-actions">{isAdmin && reservation.status === 'PENDING' && <><button onClick={onApprove}>Aceitar</button><button className="danger" onClick={onDecline}>Recusar</button></>}{!isAdmin && reservation.status === 'PENDING' && <><button onClick={onEdit}>Editar</button><button className="danger" onClick={onDelete}>Cancelar</button></>}</div></div> }
function Payments({ payments, updatePayment }) { return <section className="list-card"><SectionTitle title="Controle financeiro" /><p className="inline-tip">O pagamento integral é criado somente após a reserva ser aprovada.</p>{payments.length ? payments.map((p) => <div className="reservation-row detailed" key={p.id}><div className="pet-dot money">◈</div><div><strong>{p.petName}</strong><small>PIX · {formatCurrency(p.amount)}</small></div><span className={`status ${p.status.toLowerCase()}`}>{labelOf(p.status)}</span><div className="row-actions">{p.status === 'PENDING' && <><button onClick={() => updatePayment(p.id, 'confirm')}>Confirmar pagamento</button><button className="danger" onClick={() => updatePayment(p.id, 'cancel')}>Cancelar</button></>}</div></div>) : <Empty text="Nenhum pagamento pendente." />}</section> }
function Editor({ editor, pets, onClose, onSave, loading }) { const { type, item } = editor; const initial = item ?? (type === 'pet' ? { name: '', species: 'DOG', breed: '', birthDate: '' } : { petId: pets[0]?.id ?? '', serviceType: 'HOSTING_24H', checkInDate: '', checkOutDate: '', checkInTime: '08:00', checkOutTime: '18:00', notes: '' }); const [form, setForm] = useState(initial); const selectedPet = pets.find((pet) => String(pet.id) === String(form.petId)); const availableServices = selectedPet?.species === 'DOG' ? services.filter(([id]) => !id.startsWith('CAT_SITTER')) : services; return <div className="modal-backdrop"><form className="editor-modal" onSubmit={(e) => { e.preventDefault(); onSave(form) }}><div className="modal-heading"><div><p className="eyebrow">{item ? 'ATUALIZAR' : 'NOVA SOLICITAÇÃO'}</p><h2>{type === 'pet' ? 'Pet' : 'Serviço'}</h2></div><button type="button" className="icon-button" onClick={onClose}>×</button></div>{type === 'pet' ? <><Field label="Nome"><input required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} /></Field><Field label="Espécie"><select value={form.species} onChange={(e) => setForm({ ...form, species: e.target.value })}>{species.map((v) => <option key={v} value={v}>{labelOf(v)}</option>)}</select></Field><Field label="Raça"><input value={form.breed ?? ''} onChange={(e) => setForm({ ...form, breed: e.target.value })} /></Field></> : <><Field label="Pet"><select value={form.petId} disabled={Boolean(item)} onChange={(e) => { const pet = pets.find((value) => String(value.id) === e.target.value); const allowed = pet?.species === 'DOG' ? services.filter(([id]) => !id.startsWith('CAT_SITTER')) : services; setForm({ ...form, petId: e.target.value, serviceType: allowed.some(([id]) => id === form.serviceType) ? form.serviceType : allowed[0][0] }) }}>{pets.map((p) => <option key={p.id} value={p.id}>{p.name}</option>)}</select></Field><Field label="Serviço"><select value={form.serviceType} onChange={(e) => setForm({ ...form, serviceType: e.target.value })}>{availableServices.map(([id, name, price]) => <option key={id} value={id}>{name} — {price}</option>)}</select></Field><Field label="Data de entrada"><input required type="date" min={today()} value={form.checkInDate} onInput={(e) => setForm({ ...form, checkInDate: e.target.value })} /></Field><Field label="Horário de entrada"><input required type="time" value={form.checkInTime ?? ''} onChange={(e) => setForm({ ...form, checkInTime: e.target.value })} /></Field><Field label="Data de saída"><input required type="date" min={form.checkInDate || today()} value={form.checkOutDate} onInput={(e) => setForm({ ...form, checkOutDate: e.target.value })} /></Field><Field label="Horário de saída"><input required type="time" value={form.checkOutTime ?? ''} onChange={(e) => setForm({ ...form, checkOutTime: e.target.value })} /></Field><Field label="Observações"><textarea value={form.notes ?? ''} onChange={(e) => setForm({ ...form, notes: e.target.value })} /></Field></>}<button className="primary-button" disabled={loading}>{loading ? 'Salvando...' : type === 'pet' ? 'Salvar pet' : 'Enviar solicitação'}</button></form></div> }
function Field({ label, children }) { return <label className="field">{label}{children}</label> }; function SectionTitle({ title }) { return <div className="section-heading"><div><p className="eyebrow">GERENCIE</p><h2>{title}</h2></div></div> }; function SectionHeading({ title, action, onAction, disabled }) { return <div className="section-heading"><div><p className="eyebrow">GERENCIE</p><h2>{title}</h2></div><button className="add-button" disabled={disabled} onClick={onAction}>+ {action}</button></div> }; function Empty({ text }) { return <div className="empty-state">{text}</div> }; function ReservationRow({ reservation }) { return <article className="reservation-card"><div className="reservation-head"><div><h3>{reservation.petName}</h3><p>{serviceName(reservation.serviceType)} · {formatCurrency(reservation.totalAmount)}</p></div><span className={`status ${reservation.status.toLowerCase()}`}>{labelOf(reservation.status)}</span></div><p className="reservation-dates">{formatDate(reservation.checkInDate)} {formatTime(reservation.checkInTime)} — {formatDate(reservation.checkOutDate)} {formatTime(reservation.checkOutTime)}</p>{reservation.notes && <p className="muted">{reservation.notes}</p>}{reservation.declineReason && <p className="decline-reason">Motivo: {reservation.declineReason}</p>}</article> }; function Stat({ icon, label, value, tone }) { return <article className={`stat-card ${tone}`}><span>{icon}</span><div><small>{label}</small><strong>{value}</strong></div></article> }
function labelOf(v) { return ({ DOG: 'Cachorro', CAT: 'Gato', BIRD: 'Pássaro', OTHER: 'Outro', PENDING: 'Pendente', AWAITING_PAYMENT: 'Aguardando pagamento', CONFIRMED: 'Confirmada', DECLINED: 'Recusada', CANCELLED: 'Cancelada', PAID: 'Confirmado', PIX: 'PIX' })[v] ?? v }; function serviceName(id) { return services.find(([key]) => key === id)?.[1] ?? id }; function formatDate(v) { return new Intl.DateTimeFormat('pt-BR', { day: '2-digit', month: 'short' }).format(new Date(`${v}T12:00`)) }; function formatTime(v) { return v ? `às ${v.slice(0, 5)}` : '' }; function formatCurrency(v) { return new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(v ?? 0) }; function today() { return new Date().toISOString().slice(0, 10) }; function calendarDays(c) { const start = new Date(c.getFullYear(), c.getMonth(), 1); start.setDate(start.getDate() - start.getDay()); return Array.from({ length: 42 }, (_, i) => new Date(start.getFullYear(), start.getMonth(), start.getDate() + i)) }; function between(day, from, to) { const value = day.toISOString().slice(0, 10); return value >= from && value < to }
export default App
